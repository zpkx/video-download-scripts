# Docker 环境变量配置
# 复制此文件为 .env 并根据你的系统调整 UID/GID

# 用户 UID 和 GID (避免权限问题)
# 使用 'id -u' 和 'id -g' 命令查看你的 UID/GID
USER_UID=1000
USER_GID=1000

# 常见系统的默认值:
# Linux: USER_UID=1000, USER_GID=1000
# macOS: USER_UID=501, USER_GID=20
# WSL2: USER_UID=1000, USER_GID=1000

# 获取当前用户 UID/GID 的命令:
# echo "USER_UID=$(id -u)" > .env
# echo "USER_GID=$(id -g)" >> .env
